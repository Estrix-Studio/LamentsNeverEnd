# Asset Store Demo Checklist

- Include a minimal scene with a runner, graph, target, and at least four zone prefabs.
- Include a mixed-size zone demo.
- Include an endless weighted-pool demo.
- Include a conversion example showing existing project zones configured with `LevelZone` and `LevelExit`.
- Verify the package imports into a clean Unity 2022 LTS project.
- Verify runtime assembly has no URP, Yarn Spinner, or Input System dependency.
